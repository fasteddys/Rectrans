<Window x:Class="Rectrans.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Rectrans"
        mc:Ignorable="d"
        Title="Rectrans" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="语言">
                <MenuItem Header="源语言">
                    <MenuItem Header="英语"/>
                </MenuItem>
                <MenuItem Header="目标语言">
                    <MenuItem Header="简体中文"/>
                </MenuItem>
            </MenuItem>

            <MenuItem Header="自动翻译" x:Name="AutoTrans_MI" Click="AutoTrans_MI_Click">
                <MenuItem Header="停止"/>
                <MenuItem Header="2s"/>
                <MenuItem Header="4s"/>
                <MenuItem Header="6s"/>
                <MenuItem Header="8s"/>
                <MenuItem Header="10s"/>
            </MenuItem>

            <MenuItem Header="重置区域" x:Name="RectReset_Btn" Click="RectReset_Click"  IsEnabled="False"/>

            <MenuItem Header="窗口置顶" x:Name="TopMost_MI" Click="TopMost_Click"/>
        </Menu>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Content="设置区域" x:Name="Rect_Btn" Click="Rect_Click"/>
                <Button Content="开始翻译" x:Name="Trans_Btn" Click="Trans_Click" IsEnabled="False"/>
            </ToolBar>
        </ToolBarTray>

        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock Margin="10,0,0,0" x:Name="StatusBar" Text="字数统计(字节): 0"/>
        </StatusBar>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="原文：" HorizontalAlignment="Left" VerticalAlignment="Top" Width="80" Height="25"/>
            <TextBlock HorizontalAlignment="Stretch"
                   VerticalAlignment="Top"
                   Margin="20,20"
                   Padding="10"
                   TextWrapping="Wrap"
                   Text="Waiting..."
                   FontSize="16"
                   Height="140"
                   x:Name="OriginalTextBlock"/>

            <Label Grid.Row="1" Content="译文：" HorizontalAlignment="Left" VerticalAlignment="Top" Width="80" Height="25"/>
            <TextBlock Grid.Row="1" HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Margin="20,20"
                   Padding="10"
                   TextWrapping="Wrap"
                   Text="Waiting..."
                   FontSize="16"
                   Height="140" Width="700"
                   x:Name="TranslatedTextBlock"/>
        </Grid>
    </DockPanel>
</Window>
