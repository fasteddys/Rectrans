<UserControl x:Class="Rectrans.Views.TranslationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Rectrans.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:TranslationViewModel}"
             mc:Ignorable="d">

    <Grid Margin="10">

        <ScrollViewer HorizontalAlignment="Center" VerticalAlignment="Center"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">

            <TextBlock TextWrapping="Wrap"
                       Width="{Binding TextBlockWidth, FallbackValue=550}"
                       FontSize="{Binding FontSize, FallbackValue=20}"
                       Text="{Binding TranslationText}" />

        </ScrollViewer>

    </Grid>
</UserControl>