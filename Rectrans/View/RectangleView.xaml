<Window x:Class="Rectrans.View.RectangleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Rectrans.ViewModel"
        xmlns:local="clr-namespace:Rectrans.Controls"
        mc:Ignorable="d"
        Height="200" Width="500"
        Opacity="{Binding Opacity}"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource WindowStyle.Rect}"
        WindowChrome.WindowChrome="{DynamicResource WindowChrome}"
        Title="翻译区域">

    <Window.DataContext>
        <vm:RectangleViewModel />
    </Window.DataContext>

    <Window.Resources>
        <WindowChrome x:Key="WindowChrome">
            <WindowChrome.ResizeBorderThickness>
                <Thickness>10</Thickness>
            </WindowChrome.ResizeBorderThickness>
        </WindowChrome>
    </Window.Resources>
    
    <local:MessageBorder VerticalAlignment="Bottom" />
</Window>