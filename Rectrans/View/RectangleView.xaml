<Window x:Class="Rectrans.View.RectangleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Rectrans.ViewModel"
        mc:Ignorable="d"
        Top="{Binding Y, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
        Left="{Binding X, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
        Width="{Binding Width, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged, FallbackValue=500}"
        Height="{Binding Height, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged, FallbackValue=200}"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource WindowStyle.Rect}"
        WindowChrome.WindowChrome="{DynamicResource WindowChrome}"
        Title="翻译区域">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <WindowChrome x:Key="WindowChrome">
            <WindowChrome.ResizeBorderThickness>
                <Thickness>10</Thickness>
            </WindowChrome.ResizeBorderThickness>
        </WindowChrome>
    </Window.Resources>
</Window>